# Legacy configuration - now using application.yml
# This file is kept for backward compatibility
# Main configuration is in application.yml

# Override keystore location for development
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=${PASS:changeit}
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=artemis-dev

# Fixed development credentials for testing
spring.security.user.name=admin
spring.security.user.password=Admin123!